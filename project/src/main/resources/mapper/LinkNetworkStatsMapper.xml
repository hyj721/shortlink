<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.uestc.shortlink.project.dao.mapper.LinkNetworkStatsMapper">

    <insert id="shortLinkNetworkStats" parameterType="com.uestc.shortlink.project.dao.entity.LinkNetworkStatsDO">
        INSERT INTO t_link_network_stats (full_short_url, gid, date, cnt, network, create_time, update_time, del_flag)
        VALUES (#{linkNetworkStats.fullShortUrl}, #{linkNetworkStats.gid}, #{linkNetworkStats.date}, #{linkNetworkStats.cnt}, #{linkNetworkStats.network}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE cnt = cnt + #{linkNetworkStats.cnt}, update_time = NOW()
    </insert>

    <!-- 查询指定日期范围内的网络访问统计 -->
    <select id="listNetworkStatsByShortLink" resultType="HashMap">
        SELECT network, SUM(cnt) AS cnt
        FROM t_link_network_stats
        WHERE gid = #{param.gid}
          AND full_short_url = #{param.fullShortUrl}
          AND date BETWEEN #{param.startDate} AND #{param.endDate}
          AND del_flag = 0
        GROUP BY network
    </select>

</mapper>
