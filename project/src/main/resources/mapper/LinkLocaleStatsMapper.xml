<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.uestc.shortlink.project.dao.mapper.LinkLocaleStatsMapper">

    <insert id="shortLinkLocaleStats" parameterType="com.uestc.shortlink.project.dao.entity.LinkLocaleStatsDO">
        INSERT INTO t_link_locale_stats (full_short_url, gid, date, cnt, province, city, adcode, country, create_time, update_time, del_flag)
        VALUES (#{linkLocaleStats.fullShortUrl}, #{linkLocaleStats.gid}, #{linkLocaleStats.date}, #{linkLocaleStats.cnt}, #{linkLocaleStats.province}, #{linkLocaleStats.city}, #{linkLocaleStats.adcode}, #{linkLocaleStats.country}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE cnt = cnt + #{linkLocaleStats.cnt}, update_time = NOW()
    </insert>

    <!-- 查询指定日期范围内的地区访问统计（按省份汇总） -->
    <select id="listLocaleByShortLink" resultType="HashMap">
        SELECT province, SUM(cnt) AS cnt
        FROM t_link_locale_stats
        WHERE gid = #{param.gid}
          AND full_short_url = #{param.fullShortUrl}
          AND date BETWEEN #{param.startDate} AND #{param.endDate}
          AND del_flag = 0
        GROUP BY province
    </select>

</mapper>
