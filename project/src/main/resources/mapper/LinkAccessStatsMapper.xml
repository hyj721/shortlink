<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.uestc.shortlink.project.dao.mapper.LinkAccessStatsMapper">

    <insert id="shortLinkAccessStats" parameterType="com.uestc.shortlink.project.dao.entity.LinkAccessStatsDO">
        INSERT INTO t_link_access_stats (gid, full_short_url, date, pv, uv, uip, hour, weekday, create_time, update_time, del_flag)
        VALUES (#{linkAccessStats.gid}, #{linkAccessStats.fullShortUrl}, #{linkAccessStats.date}, #{linkAccessStats.pv}, #{linkAccessStats.uv}, #{linkAccessStats.uip}, #{linkAccessStats.hour}, #{linkAccessStats.weekday}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE pv = pv + #{linkAccessStats.pv}, uv = uv + #{linkAccessStats.uv}, uip = uip + #{linkAccessStats.uip}, update_time = NOW()
    </insert>

    <!-- 查询指定日期范围内的基础访问统计列表 -->
    <select id="listStatsByShortLink" resultType="com.uestc.shortlink.project.dao.entity.LinkAccessStatsDO">
        SELECT id, gid, full_short_url, date, pv, uv, uip, hour, weekday
        FROM t_link_access_stats
        WHERE gid = #{param.gid}
          AND full_short_url = #{param.fullShortUrl}
          AND date BETWEEN #{param.startDate} AND #{param.endDate}
          AND del_flag = 0
    </select>

</mapper>
